# python3 -m venv .venv
# source .venv/bin/activate
pip install -r requirements.txt

# webencodings, pytz, python-decouple, websockets, uvloop, tzdata, typing-extensions, sniffio, six, 
# pyyaml, python-dotenv, psycopg2-binary, polars, numpy, idna, httptools, h11, greenlet, click, 
# asyncpg, uvicorn, sqlalchemy, python.dateutil, pydantic, pyarrow, bleach, anyio, watchfiles, 
# starlette, pandas, fastapi